---
import TaxesDocumentHeader from '../../../../components/l3/react/taxes/TaxesDocumentHeader';
import TaxesRelatedDocs from '../../../../components/l3/solid/taxes/TaxesRelatedDocs';
import TaxesPdfLink from '../../../../components/l3/svelte/taxes/TaxesPdfLink.svelte';
import TaxesOwnerDetails from '../../../../components/l3/vue/taxes/TaxesOwnerDetails.vue';
import { type DeedRecord, deeds } from '../../../../data/taxes/deeds';
import '../../../../styles/l3/taxes.css';
import L3Guard from '../../../../components/l3/L3Guard.astro';
import ThemeInit from '../../../../components/ThemeInit.astro';
import ThemeToggle from '../../../../components/ThemeToggle.astro';

export async function getStaticPaths() {
	return deeds.map((deed) => ({
		params: { id: deed.fileNum },
		props: { deed },
	}));
}

interface Props {
	deed: DeedRecord;
}
const { deed } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{deed.fileNum} — Eldoria Registry of Deeds L3</title>
    <ThemeInit />
  </head>
  <body>
    <header class="er3-masthead">
      <div class="er3-masthead-inner">
        <span class="er3-crest">🛡</span>
        <div class="er3-title-block">
          <span class="er3-title-main">Eldoria Registry of Deeds</span>
          <span class="er3-tagline">Official Property Records · L3 Islands Edition</span>
        </div>
        <span class="er3-edition">QScrape · L3</span>
      </div>
      <nav class="er3-nav">
        <div class="er3-nav-inner">
          <a href="/l3/taxes/" class="er3-nav-item">Search</a>
          <a href="/l3/" class="er3-nav-item">← L3</a>
        </div>
      </nav>
    </header>

    <main class="er3-main">
      <div class="er3-viewer-layout">
        <div class="er3-viewer-primary">
          <TaxesDocumentHeader client:only="react" deed={deed} />
          <TaxesOwnerDetails client:only="vue" fileNum={deed.fileNum} />
        </div>
        <aside class="er3-viewer-sidebar">
          <TaxesPdfLink client:only="svelte" fileNum={deed.fileNum} />
          <TaxesRelatedDocs client:only="solid-js" fileNum={deed.fileNum} />
        </aside>
      </div>
    </main>

    <footer class="er3-footer">
      <div class="er3-footer-inner">
        <span>© 2027 Eldoria Registry of Deeds</span>
        <span>QScrape L3 · Islands: React · Vue · Svelte · Solid</span>
      </div>
    </footer>
    <ThemeToggle />
    <L3Guard />
  </body>
</html>

<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: var(--er3-bg);
    color: var(--er3-text);
    font-family: var(--er3-font);
    font-size: 16px;
    line-height: 1.6;
  }

  .er3-masthead {
    background: var(--er3-surface);
    border-bottom: 1px solid var(--er3-border);
  }
  .er3-masthead-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 16px 24px;
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 16px;
  }

  .er3-crest {
    font-size: 28px;
    line-height: 1;
    filter: hue-rotate(160deg);
  }

  .er3-title-block {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .er3-title-main {
    display: block;
    font-family: var(--er3-font);
    font-size: clamp(18px, 2.5vw, 28px);
    font-weight: 700;
    letter-spacing: -0.3px;
    line-height: 1.1;
    color: var(--er3-primary);
  }
  .er3-tagline {
    display: block;
    font-size: 11px;
    color: var(--er3-muted);
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }
  .er3-edition {
    font-family: var(--er3-font);
    font-size: 11px;
    color: var(--er3-muted);
    text-align: right;
    font-style: italic;
  }

  .er3-nav {
    border-bottom: 2px solid var(--er3-primary);
    background: var(--er3-surface2);
  }
  .er3-nav-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 24px;
    display: flex;
  }
  .er3-nav-item {
    padding: 10px 16px;
    font-family: var(--er3-font);
    font-size: 13px;
    font-weight: 500;
    color: var(--er3-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-decoration: none;
    border-bottom: 3px solid transparent;
    margin-bottom: -2px;
    transition: color 0.15s;
  }
  .er3-nav-item:hover { color: var(--er3-text); }

  .er3-main {
    max-width: 1100px;
    margin: 0 auto;
    padding: 32px 24px;
  }

  .er3-viewer-layout {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 40px;
  }
  @media (max-width: 768px) {
    .er3-viewer-layout { grid-template-columns: 1fr; }
  }

  .er3-viewer-primary {
    display: flex;
    flex-direction: column;
    gap: 24px;
    min-width: 0;
  }
  .er3-viewer-sidebar {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .er3-footer {
    border-top: 1px solid var(--er3-border);
    background: var(--er3-surface);
    margin-top: 48px;
  }
  .er3-footer-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 16px 24px;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 8px;
    font-family: var(--er3-font);
    font-size: 12px;
    color: var(--er3-muted);
  }
</style>

---
interface Props {
  status: 'live' | 'upcoming' | 'final';
}
const { status } = Astro.props;
const labels: Record<string, string> = {
  live: 'LIVE',
  upcoming: 'UPCOMING',
  final: 'FINAL',
};
---
<span class={`event-badge ${status}`}>{labels[status]}</span>
